<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="manufactureStage">
        <h2 class="jh-entity-heading" data-cy="manufactureStageDetailsHeading">
          <span v-text="t$('storeApp.manufactureStage.detail.title')"></span> {{ manufactureStage.id }}
        </h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="t$('storeApp.manufactureStage.name')"></span>
          </dt>
          <dd>
            <span>{{ manufactureStage.name }}</span>
          </dd>
          <dt>
            <span v-text="t$('storeApp.manufactureStage.description')"></span>
          </dt>
          <dd>
            <span>{{ manufactureStage.description }}</span>
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="t$('entity.action.back')"></span>
        </button>
        <router-link
          v-if="manufactureStage.id"
          :to="{ name: 'ManufactureStageEdit', params: { manufactureStageId: manufactureStage.id } }"
          custom
          v-slot="{ navigate }"
        >
          <button @click="navigate" class="btn btn-primary">
            <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span></span>
          </button>
        </router-link>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./manufacture-stage-details.component.ts"></script>
